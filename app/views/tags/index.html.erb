<%= link_to "<< Back to all Posts", posts_path %>
<h1>All Tags</h1>

<div><%= link_to "Create a Tag", new_tag_path %></div><br>

<div class="tags">
  <% @tags.each do |tag| %>
    <span style="border:outset"><%= tag.name %></span>
  <% end %>
</div>
